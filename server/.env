# --- Database Credentials (using individual variables) ---
# These are used if DATABASE_URL below is commented out or missing
DB_HOST=localhost
DB_PORT=5432
DB_NAME=link_shortener
DB_USER=taroo
DB_PASSWORD=1234 # No quotes needed unless password has special chars/spaces

# --- Optional: Full Database Connection URL ---
# If you uncomment this line, it will be used INSTEAD of the individual DB_* variables above
# Ensure the username, password, host, port, and dbname match!
# DATABASE_URL=postgresql://taroo:1234@localhost:5432/link_shortener

# --- Database SSL Configuration ---
# Set to "true" if your database connection requires SSL, otherwise "false" or omit
DB_SSL=false

# --- Application Environment ---
# Set to "production" for production builds, "development" otherwise
# Used by Drizzle logger and potentially other libraries
NODE_ENV=development

# --- Server Port ---
# The port your Express server will listen on (used in server/src/index.ts)
PORT=4000

# --- Frontend URL (for CORS) ---
# The URL of your frontend application (used in server/src/index.ts for CORS middleware)
# Adjust if your frontend runs on a different port or domain
FRONTEND_URL=http://localhost:5173

# --- Corresponding Frontend Variable (Reminder) ---
# Your frontend app likely needs this variable in its own .env file:
# VITE_API_URL=http://localhost:4000/api